# UI refresh summary

The primary UI now follows Carbon's dedicated-page form pattern, uses three linear steps, concise labels, 40px inputs, a responsive 16-column grid, automatic YAMAP analysis, automatic Nagano police lookup, and optional local persistence for planner/base contacts.
