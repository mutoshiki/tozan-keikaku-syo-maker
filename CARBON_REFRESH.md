# Carbon refresh

- 3 steps only: 基本情報 / YAMAP / 確認
- Visible copy is task text only; avoid implementation or system explanations.
- Default input height: 40px; top-aligned concise labels.
- Dedicated-page layout follows a responsive 16-column grid.
- Known values are prefilled or detected instead of repeatedly entered.
- YAMAP upload triggers analysis automatically; no separate analyze action.
- Police station is derived from detected route municipalities and remains editable for correction.
- Planner and base-contact details can be saved locally with one checkbox.
- PDF source stays off-screen; the UI does not duplicate the document as a large preview.
